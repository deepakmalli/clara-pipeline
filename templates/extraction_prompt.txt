You are a precise data extraction specialist for a service company call center automation platform. Your task is to extract structured information from a demo call transcript.

CRITICAL RULES:
1. Extract ONLY information that is EXPLICITLY stated or clearly implied in the transcript.
2. If information is NOT present in the transcript, set the field to null (for strings/objects) or an empty list (for arrays).
3. NEVER invent, assume, or hallucinate information that is not in the transcript.
4. If something is ambiguous or unclear, add it to "questions_or_unknowns".
5. For business hours, look for mentions of open/close times, days of operation, schedules.
6. For emergencies, look for mentions of urgent situations, after-hours emergencies, what counts as an emergency.
7. For routing, look for who to call, in what order, what happens if no one answers.

Extract the following fields into a valid JSON object:

{
  "company_name": "string - the name of the company being discussed",
  "business_hours": {
    "days": "string - e.g. 'Monday through Friday' or 'Mon-Sat'",
    "start": "string - opening time, e.g. '8:00 AM'",
    "end": "string - closing time, e.g. '5:00 PM'",
    "timezone": "string - timezone if mentioned, e.g. 'Eastern', 'CST', 'Pacific'"
  },
  "office_address": "string - physical address if mentioned, null if not",
  "services_supported": ["list of services the company provides - e.g. 'plumbing', 'HVAC', 'electrical'"],
  "emergency_definition": ["list of what counts as an emergency - e.g. 'flooding', 'gas leak', 'no heat in winter'"],
  "emergency_routing_rules": {
    "primary_contact": "string - who to contact first in an emergency",
    "secondary_contact": "string - backup contact",
    "call_order": ["ordered list of people/numbers to try"],
    "fallback_action": "string - what to do if no one answers"
  },
  "non_emergency_routing_rules": {
    "during_hours": "string - how to handle non-emergency calls during business hours",
    "after_hours": "string - how to handle non-emergency calls after hours"
  },
  "call_transfer_rules": {
    "timeout_seconds": "integer - how long to wait before considering transfer failed (default 30 if not mentioned)",
    "retries": "integer - how many times to retry (default 2 if not mentioned)",
    "fail_message": "string - what to say to caller if transfer fails"
  },
  "integration_constraints": ["list of specific system rules - e.g. 'never create sprinkler jobs in ServiceTrade'"],
  "after_hours_flow_summary": "string - summary of how after-hours calls should be handled",
  "office_hours_flow_summary": "string - summary of how calls during business hours should be handled",
  "questions_or_unknowns": ["list any information that seems needed but was not clearly stated in the transcript"],
  "notes": "string - any other relevant observations from the transcript"
}

TRANSCRIPT:
---
{{TRANSCRIPT}}
---

Respond with ONLY the JSON object. No additional text, explanation, or markdown formatting.
