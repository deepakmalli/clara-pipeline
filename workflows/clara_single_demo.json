{
  "name": "Clara Pipeline - Single Demo Call",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "clara/demo",
        "responseMode": "responseNode",
        "options": {}
      },
      "id": "webhook-demo",
      "name": "Demo Webhook",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 1,
      "position": [240, 300],
      "webhookId": "clara-demo-webhook",
      "notes": "POST /webhook/clara/demo with { \"file_path\": \"dataset/demo/call1.txt\" }"
    },
    {
      "parameters": {
        "command": "={{ 'cd /data && python3 run_pipeline.py demo --input ' + $json.body.file_path + ' 2>&1' }}"
      },
      "id": "run-demo-pipeline",
      "name": "Run Pipeline A",
      "type": "n8n-nodes-base.executeCommand",
      "typeVersion": 1,
      "position": [500, 300],
      "notes": "Execute Pipeline A for a single demo call"
    },
    {
      "parameters": {
        "options": {
          "responseCode": 200
        },
        "respondWith": "json",
        "responseBody": "={{ JSON.stringify({ status: 'success', output: $json.stdout }) }}"
      },
      "id": "respond-demo",
      "name": "Respond",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1,
      "position": [740, 300]
    }
  ],
  "connections": {
    "Demo Webhook": {
      "main": [
        [
          { "node": "Run Pipeline A", "type": "main", "index": 0 }
        ]
      ]
    },
    "Run Pipeline A": {
      "main": [
        [
          { "node": "Respond", "type": "main", "index": 0 }
        ]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1"
  },
  "tags": [
    { "name": "clara-pipeline" }
  ],
  "triggerCount": 1,
  "updatedAt": "2026-03-03T00:00:00.000Z",
  "versionId": "1"
}
